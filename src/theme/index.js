import colors from './colors'

const themes = {
  light: {
    colors: {
      background: colors.white,
      text: colors.black,
      title: colors.black,
      quotes: colors.gray
    }
  },
  dark: {
    colors: {
      background: colors.black,
      text: colors.white,
      title: colors.green,
      quotes: colors.blue
    }
  }
}

export default themes
